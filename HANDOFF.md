# 作品集网站改版交接文档

> 写给完全没有历史上下文的新对话。请先完整阅读本文和 `CONVERSATION_ARCHIVE.md`，再修改代码。
> 最后更新：2026-07-22
> 本地目录：`/Users/Admin/my-portfolio-main`
> GitHub：`chen1041297839-maker/my-portfolio`
> 当前工作分支：`second`（不要直接修改或推送 `main`）
> 当前网站代码提交：`39a7223 Refine portfolio case study experience`

## 1. 我们在做什么

这是陈馨语（Xinyu Chen）的个人作品集网站，主要用于 UI/UX、体验设计、复杂 B 端产品与 AI 产品方向的面试展示。

她的经历横跨：

- AI 产品与复杂 B 端系统；
- UI/UX 与交互体验；
- 游戏体验；
- 空间叙事、数字媒体和实时交互装置。

改版目标不是把她包装成“什么都会的多边形设计师”，而是建立一条清楚的职业主线：

> 通过产品结构、交互反馈与空间叙事，设计人如何理解复杂系统、如何在其中行动，以及这个过程产生什么感受。

网站主要面对面试官和设计负责人。首页需要在 5—8 秒内回答“她是谁、擅长什么、与普通 UI 设计师有什么不同”，并让观看者在 2—5 分钟内找到最有说服力的项目。

## 2. 已确认的设计方向

### 2.1 核心主题

主题是 **感知系统主义 / Perceptual Systemism**。

- “系统”对应复杂产品中的结构、角色、状态、任务、规则和反馈；
- “感知”对应空间、游戏与跨媒介背景中的节奏、注意力、参与感和情绪；
- 目标是同时保留商业产品的可信度与个人设计表达。

首页核心主张：

> 我整理复杂产品，也制造不止发生在屏幕里的体验。

### 2.2 视觉系统

- 空间黑 `#0C0B10`：不是纯黑，作为数字展览的舞台；
- 冷白 `#F4F0F7`：保证投屏和案例阅读的清晰度；
- 感知紫 `#875CFF`：空间、实时媒介、状态与品牌膜层；
- 信号橙 `#FF6433`：只用于联系、关键动作和转场，不应泛滥；
- 结构线 `#302B36`：建立坐标、测量和秩序感。

视觉不是通用黑蓝紫“科技模板”，也不是全站玻璃拟态。首页借用了数字展览、空间界面和空间计算中对层级、焦点、遮挡的处理，但不能直接复制 Apple、Linear、Vercel 或 Spencer Gabor。

首屏紫色膜层是唯一的品牌签名之一：需要自然穿过画面，不能出现硬切线、短线穿梭或僵硬机械运动。

### 2.3 排版与布局

- 中文以清晰的系统无衬线为主；
- Barlow Semi Condensed 用于英文导览；
- Rajdhani 只用于项目编号、状态和少量转场；
- 使用“双尺度”原则：一个有力量的大观点 + 一组精确的小信息；
- 通过字号差、留白和不对称关系形成设计感，不依赖渐变字或装饰字体；
- 作品本身要领先于容器，避免把所有模块都做成同一种圆角卡片。

## 3. 当前首页信息结构

首页项目不是 14 个等权平铺，而是分层策展：

1. **Selected Work / 精选项目（3 个）**
   - Hologrow WebApp；
   - Hologrow 官网；
   - Hi 链接解析。
2. **More Product Work（3 个）**
   - 包的 AI；
   - 待办薯 AI 协作；
   - Top Fax Review。
3. **Experiments & Other Media（2 个）**
   - e-dentity；
   - echo-harmony。
4. **Archive**
   - 只展示前面未出现的剩余项目；
   - 不允许展开后再次重复全部项目。

精选项目使用“中心展品 + 两侧空间预览”的结构。悬停或点击其他项目时，那个项目应真正移动到中间成为当前焦点；不要做半透明叠图，也不要让纵向滚动映射成横向切换。

## 4. 已经完成的工作

### 4.1 首页与通用交互

- 完成数字展览方向的首页重构；
- 完成黑、紫、橙品牌系统与首屏膜层；
- 完成 3 + 3 + 2 + Archive 的项目层级；
- 精选项目的数字分页、左右箭头、键盘方向键、拖拽与悬停切换可用；
- 项目卡片整体作为点击区域，不应只能点击一小块文字；
- 打开项目、关闭项目、浏览器前进后退、刷新和返回位置恢复已有专门逻辑；
- `?present=1` 演示模式会隐藏数字分身并减弱干扰性动效；
- 正常模式保留 Gemini 数字分身；
- Archive 支持分类过滤；
- 图片灯箱支持独立于浏览器缩放的放大、缩小、拖拽和滚动；放大后图片可以大于屏幕，关闭按钮固定在视口右上角；
- 对 `prefers-reduced-motion` 做了适配。

### 4.2 二级案例页

以下五个重点项目已经从“PDF 图片列表”升级为网页叙事结构：

- Hologrow WebApp：`src/cases/HologrowWebAppCase.jsx`；
- Hologrow 官网；
- Hi 链接解析；
- 待办薯；
- 包的 AI。

后四个共用 `src/cases/NarrativeCaseStudy.jsx`，根据项目 ID 使用不同的内容配置与项目视觉气候。

案例页的基本原则：

> 网页原生文字讲结论，图片、真实界面和 Live Frame 提供证据。

已经包括：

- 项目封面、角色、时间和范围；
- 章节导航；
- 背景、问题、策略、关键判断和复盘；
- 项目专属颜色，而不是所有二级页纯黑同构；
- Hologrow 官网入口 `https://hologrow.ai/`；
- Hi 链接解析和待办薯的 Figma Live Frame；
- Live Frame 左上角的 `LIVE` 标识；
- 不再需要“启用交互”开关；
- Hologrow WebApp 时间已改为 `2026.6—2026.7`；
- Hologrow WebApp 的 Integrations、Usage、Team Management、对象模型和设计系统等内容已拆分表达；
- Hologrow 最新高清过程图和官网过程图已经加入 `public/projects/hologrow/`，展示时不应裁切重要信息。

### 4.3 Gemini 数字分身

- 从旧版本恢复 Gemini 作品集问答；
- API 调用逻辑没有删除；
- 已修复回答被截断的问题；
- 已支持 Markdown 渲染；
- 演示模式隐藏，普通模式保留。

相关提交：

- `5f97787 Restore Gemini portfolio guide securely`
- `befbd5a Prevent truncated AI guide responses`
- `ee9c71c Render AI responses as Markdown`

### 4.4 设计上下文文档

- `PRODUCT.md`：产品定位、用户、内容策略、项目层级和约束；
- `DESIGN.md`：视觉系统、主题、色彩、字体、布局和动效原则；
- `.impeccable/design.json`：设计系统上下文（已在更早提交中上传）。

新对话在改版前必须先读 `PRODUCT.md` 和 `DESIGN.md`。

## 5. 当前卡在哪里 / 尚未完成

目前没有技术阻塞，生产构建通过；当前停在 **内容与版式的最后一轮精修和逐项目视觉验收**。

最新一条明确未落实的反馈：

### Hi 链接解析的 URL 配置段文案重复

同一个章节里：

- 上方大标题与右侧说明已经讲了“先注册 URL 规则，再向下验证”；
- Live Frame 左侧的“URL 规则配置”标题和说明又重复讲了一遍。

下一步不能只换同义词，应该重新分工：

- 上方承担“为什么采用单线向下的配置—验证路径”；
- Live Frame 旁承担“用户在这个画板里具体完成什么，以及完成后的系统反馈是什么”。

建议文案方向：

- 上方观点保留流程判断；
- Live Frame 标题可改为“把匹配条件变成可验证的规则”；
- Live Frame 说明可改为“用户在画板中定义匹配范围、展示样式与回调地址；完成后直接用真实链接检查 IM 气泡，不需要保存后跳到另一个页面寻找结果。”

### 仍需逐项视觉检查的历史反馈

这些项目在代码中已有处理，但需要新对话在浏览器中再次确认，不能仅看源码宣布完成：

1. WebApp 设计系统图片不应过大，它是支撑证据，不是案例高潮；
2. Hi 链接解析必须先完成配置，再向下进入沙箱验证，沙箱在后面而不是左右并排；
3. Hi 链接解析的 Live Frame 应包含“增加 URL 规则”的真实原型；
4. “高效不等于把所有操作做得一样快”及对应无关大图应已经移除，若仍出现继续删除；
5. 移动端审批要说清楚：将并行按钮改为纵向排列，使主要与次要操作落在拇指热区，同时降低误触；不要写成笼统的“保留信息”；
6. 待办薯叙事顺序必须是：完整讲完配置，再进入 IM 使用场景；
7. 包的 AI 图片必须完整呈现，不能 `object-fit: cover` 裁掉关键流程；
8. Hologrow 官网二级页第一张图保留原图，后续图片按用户提供顺序替换；
9. 案例页章节导航颜色要融入项目气候，不能像突兀的整条彩色栏；
10. 1440、1280 和 390 三个视口都要重新截图检查，尤其注意中文单字落行。

## 6. 下一步计划

建议严格按以下顺序推进：

### Step 1：修复 Hi 链接解析重复文案

- 只调整配置章节上下两处文字的职责；
- 不改 Figma URL，不删 Live Frame；
- 检查文案是否和实际画板一致。

### Step 2：逐项目浏览器验收

依次打开：

- `/?project=hologrow-webapp`
- `/?project=hologrow-website`
- `/?project=hi-link`
- `/?project=todo-ai`
- `/?project=bao-ai`

检查：章节锚点、图片比例、Live Frame、文字/设计稿一致性、返回位置、图片灯箱、移动端滚动。

### Step 3：三视口验收

- 1440px：面试投屏主视口；
- 1280px：常见笔记本；
- 390px：移动端。

需要同时验证：

- 正常模式；
- `?present=1` 演示模式；
- 页面刷新；
- 浏览器前进/后退；
- 从首页不同阶段进入案例再返回；
- `/?archive=1`。

### Step 4：只修确认的问题

当前方向已经成立，不要再重做首页视觉或大规模重构数据结构。先获得用户截图确认，再进入下一轮。

## 7. 绝对不要再踩的坑

### 内容与事实

1. **禁止编造职责、业务成果、团队反馈和指标。** 信息不足时保留占位或询问用户。
2. 不要把案例重新做回“整张 PDF 缩小后依次摆放”。
3. 不要让网页文字重复图片里已经写清楚的标题和结论。
4. 不要把所有项目解释成同一种设计方法；每个项目要回答它独有的判断。
5. 不要使用 Hologrow 公司 Logo 作为陈馨语的个人 Logo。

### 项目与图片

6. **不要删除任何原始项目数据、原图或 Figma Frame。** 待办薯此前被删过多个配置 Frame，这是严重回退。
7. 替换图片前先建立明确的“文件 → 页面位置”映射；不要凭文件名猜顺序。
8. Hologrow 官网第一张图明确要求不替换。
9. 过程图、流程图和包的 AI 图片不得裁掉关键信息；优先 `contain` 或自然高度。
10. WebApp 设计系统图片不要占据过高视觉权重。

### 交互

11. 不要增加“启用交互”状态。Figma Frame 默认可操作，左上角标 `LIVE` 即可。
12. Live Frame 不要铺满整个页面导致 iframe 抢走纵向滚动；需要控制尺寸并在旁边保留解释文字。
13. 不要把纵向滚动转换成横向项目切换。
14. 精选项目悬停必须让对应项目进入中心，不要做透明叠加。
15. 所有项目卡片应整块可点击，不能只有标题或按钮的一小块可点击。
16. 关闭案例必须返回进入前的首页滚动位置，不能跳到最底部。
17. 图片放大必须独立于浏览器缩放；图片可以超过视口，关闭按钮始终固定可见。

### 视觉

18. 不要把紫色膜层限制在明显矩形框内，也不要加入穿梭短线。
19. 不要重新做成通用 Bento SaaS 首页、全玻璃墙或随机极光背景。
20. 不要让每个项目使用同样的黑底、同样的卡片和同样的颜色。
21. 项目专属色需要降低侵略性：Hologrow 的酸性绿应克制；Hi/待办薯使用项目蓝；包的 AI 不要使用突兀的亮蓝大底。
22. 注意中文换行，避免“不同尺度”“同一个问题”等词组被拆开或移动端出现单字落行。

### Git 与部署

23. 当前只在 `second` 工作，**不要直接把改动提交到 `main`**。
24. 用户说“只在本地修改”时，不要自动提交或推送；只有用户明确要求上传时才 push。
25. 不要提交本地工具目录和临时产物：`.claude/`、`.codex/`、`.cursor/`、`.playwright-cli/`、`output/`、`Close`、`.impeccable/config.local.json`。
26. 推送 `second` 不一定会更新 Vercel 正式域名。Vercel 项目的 **Production Branch** 必须设置为 `second`；否则只会生成 Preview Deployment。

## 8. 代码结构速查

- `src/App.jsx`
  - 项目数据；
  - Figma Prototype URL；
  - Gemini 数字分身和 API 调用；
  - 旧版组件仍有部分保留，不要盲目全删。
- `src/Portfolio.jsx`
  - 当前首页；
  - 精选项目轮播；
  - Archive；
  - 项目打开/关闭与 history/scroll 恢复；
  - 图片灯箱；
  - 二级页路由分发。
- `src/cases/HologrowWebAppCase.jsx`
  - Hologrow WebApp 专属案例结构。
- `src/cases/NarrativeCaseStudy.jsx`
  - Hologrow 官网、Hi 链接解析、待办薯、包的 AI 的叙事配置和页面结构。
- `src/portfolio.css`
  - 首页、案例页、灯箱、Figma Frame、响应式与项目气候的主要样式。
- `src/portfolio-overrides.css`
  - Gemini 数字分身样式和少量覆盖。
- `public/projects/hologrow/`
  - Hologrow WebApp 与官网的新旧高清图片。
- `PRODUCT.md`
  - 产品与内容策略。
- `DESIGN.md`
  - 视觉与交互原则。

## 9. 本地运行与验证

安装依赖后：

```bash
npm run dev -- --host 127.0.0.1 --port 5174
```

浏览器访问：

```text
http://127.0.0.1:5174/
```

如果出现 `ERR_CONNECTION_REFUSED`，通常不是页面代码错误，而是 Vite 进程没有运行。浏览器打开 localhost 不会自动启动开发服务器。

生产构建：

```bash
npm run build
```

截至本文写入前，`npm run build` 已通过，Vite 版本为 8.0.1。

## 10. Git 与线上状态

- GitHub 远程：`git@github.com:chen1041297839-maker/my-portfolio.git`
- 当前分支：`second`
- 网站代码最新提交：`39a7223`
- 上述提交已推送到 `origin/second`
- 用户提供的 Vercel 地址：`https://my-portfolio-ten-lemon-244d3b981v.vercel.app/`

如果线上页面没有变化：

1. 先查看 Vercel 最新 Deployment 是否来自 `second`；
2. 检查该 Deployment 是 Preview 还是 Production；
3. 在 Vercel Settings → Git 中确认 Production Branch 为 `second`；
4. 不要通过合并到 `main` 来绕过这个配置。

## 11. 接手后的第一句话应该是什么

新对话接手时，不要直接说“我会全面优化”。应该先向用户确认：

> 我已经读完 `HANDOFF.md`、`PRODUCT.md`、`DESIGN.md` 和 `CONVERSATION_ARCHIVE.md`。当前方向不需要重做；我会先核对交接文档中的未完成项，再按 Hologrow WebApp、Hologrow 官网、Hi 链接解析、待办薯、包的 AI 的顺序做浏览器验收。不会删除原图、项目数据、Figma Frame 或 Gemini 代码，也不会把改动提交到 main。
